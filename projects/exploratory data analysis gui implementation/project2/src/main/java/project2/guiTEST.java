package project2;

import java.io.IOException;
import java.nio.file.InvalidPathException;

public class guiTEST {
    public static void main(final String[] args) throws InvalidPathException, IOException{

        new main_menu();
        
        // load using getFolderPath
        // use printDB to display all databases
        // use dbSize for num of datasets 

        // display menu gui

        /* main loop choice != 0{
            if choice = 1
                if dbSize < !
                    error
                else
                    enter existing ID
                    if for newId not matching
            else if choice = 2
                if id not exists
                    enter new ID
                    prompt source name
                    if check source vaid
                        add to collection
                    else error
                else error
            else if choice = h
                open menu gui

            if (found)
                if 3
                if 4

            end while
            catch datapath not found
        //}*/
    }
}
